/*globals require:false, exports:false */
/*jshint globalstrict:true */
'use strict';

/*var pages = require('ui/page'),
	page = new pages.Page(),
	listViewModule = require('ui/list-view'),
	observableArray = require('data/observable-array'),
	labelModule = require('ui/label'),
	listView = new listViewModule.ListView(),
	colors = ['Mario', 'Luigi', 'Toad', 'Peach', 'Bowser'];

listView.items = colors;
listView.on('itemLoading', function (args) {
	if (!args.view) {
		args.view = new labelModule.Label();	
	}
	
	args.view.text = colors[args.index];
});


page.css = 'label { height: 80px; color: blue; }';
page.content = listView;
exports.Page = page;*/
	
	